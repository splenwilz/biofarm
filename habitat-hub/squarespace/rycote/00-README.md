# Rycote Farm — lean page build (v3 copy, 23 Sep 2026)

Paste each file into its own Code Block on the `rycote-farm` blog post, in order:

**01-hero → 02-overview → 03-location → 04-map → 06-units → 08-stewardship → 12-resources → 15-cta**

Set SEO title/description in post settings (in 01-hero's comment). Re-paste
`finder-block-INLINE.html` on the hub (Rycote card: region line + chip order updated).

## Before it can go fully live
1. **Imagery** — prepared in `assets/`: `rycote-hero.jpg` (drone, River Thame meanders)
   + `rycote-overview.jpg` (top-down scrub mosaic, 4:3 crop). BEST ORDER: upload both to
   the image library FIRST, send the CDN URLs, blocks get wired, then paste once.
   Spares: "Big Giles and Little Giles (3)" (parcels), Colsey.jpeg (ground-level flood),
   and an MP4 drone video ("Big Giles (2).MP4") if a video block is ever wanted.
2. **Brochure** — links point at `/s/Biofarm-Habitat-Bank-Brochure-Rycote-Farm_FINAL.pdf`
   (404 until the FINAL PDF from Vix's Drive is uploaded with that exact name).
3. **Map boundaries** — pin + indicative circle interim; South Oxfordshire LPA + Upper
   Thames Clay Vales NCA polygons restore the solid/hatched key.
