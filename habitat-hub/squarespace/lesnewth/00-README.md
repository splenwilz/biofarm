# Lesnewth — lean page build (v3 copy, 23 Sep 2026)

Paste each file into its own Code Block on the `lesnewth` blog post, in order:

**01-hero → 02-overview → 03-location → 04-map → 06-units → 08-stewardship → 12-resources → 15-cta**

Then set SEO title/description in the post settings (in 01-hero's header comment).
Also re-paste `finder-block-INLINE.html` on the hub (Lesnewth card: region line +
habitat chips updated to the listing-card copy).

## Before it can go fully live
1. **Imagery** — prepared files in `assets/`: `lesnewth-hero.jpg` (drone aerial) +
   `lesnewth-overview.jpg` (sunlit rainforest stream). Upload both to the image
   library, send the CDN URLs, and the temporary old card photo gets swapped out
   of 01-hero + 02-overview. Spares: IMG_0662 (mossy trunk/stream), IMG_0665 (lane).
2. **Brochure** — links point at `/s/Biofarm-Habitat-Bank-Brochure-Lesnewth_FINAL.pdf`
   (404 until uploaded). NOTE: Lesnewth's FINAL brochure was still awaiting Biofarm
   sign-off in Aug — confirm it exists in Vix's Drive before uploading.
3. **Map boundaries** — pin + indicative circle interim; Cornwall LPA + Cornish Killas
   NCA polygons restore the solid/hatched key (same pipeline as Sleight's map).
